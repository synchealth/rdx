{
  "name": "rdx-mono-repository",
  "description": "Reactive Dialogs (RDX) for digital conversations",
  "private": true,
  "version": "0.5.0",
  "repository": "synchealth/rdx",
  "author": {
    "name": "Internet Open Protocol Alliance (IOPA)"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "tslint": "^5.18.0",
    "typescript": "4.0.5",
    "ts-node": "^9.0.0",
    "@berun/dev-scripts": "^3.1.1"
  },
  "scripts": {
    "build": "lerna run build",
    "clean": "rimraf **/build && rimraf **/dist && rimraf **/node_modules && rm -rf yarn.lock && rm -rf .fusebox",
    "lint": "lerna run lint"
  },
  "dependencies": {},
  "resolutions": {
    "babel-core": "7.0.0-bridge.0",
    "@babel/core": "7.12.3",
    "typescript": "4.0.5"
  },
  "workspaces": [
    "packages/*"
  ]
}